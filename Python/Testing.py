def test():
    # lista zwykla rlist = ['Love', 'Life', 'G']
    # lista nonmodifacal rtuple = ('Money', 'Job', 'Sucess')
    # key->value rdictionary = {'Her': 'G', 'He': '?'}
    # ---------------
    # a, b = 2, 3
    # one line print("a" if a > b else "b")
    # ---------------
    # seed(n) random(n) randint(start, stop) randrange(start, stop, step) gauss(mean, devation)
    # choice(list) choices(list, k=4) sample(list, 6) shuffle(list)
    # ----------------
    # Slice a string a[3:8] 3->7 a[:] whole a[:-2] backwards
    # name='Aya' print(f"It isn't {name}") print("I love {0}".format(a='Aya'))
    # print("I love %s and %s" %(a, b)) -> $s string $d int $f float
    # \n linefeed \t - tab \\- backlash \' single quote \" double quoute
    # len() str() lower() upper() strip() isdigit() isalpha() isspace() startswith() endswith() find()
    # replace('na', 'ha') split() '*'.join(list) x*x*x
    # 'na' in 'banana' 'less' not in 'helpless''
    # 'Hey' is 'Hi' is not | and or not
    # ---------------
    # Btw - AND(&) OR(|) XOR(^) Complement(~) LeftShift(<<) RightShift(>>)
    # ---------------
    # bin(2) -> 0b10 oct() hex()



print(test())
